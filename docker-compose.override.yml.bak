services:
  backend:
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:4000/health | grep -F '\"status\":\"ok\"' || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

